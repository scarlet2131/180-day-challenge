1. geeksforgeeks - google
    a.) Find all triplets with zero sum- Approach
        I.) O(n^3), O(n^2)- using map , O(n^2) - using two pointer
    b.) Generate all binary strings from given pattern
        I.) Recursion , queue
    c.) Count of strings that can be formed using a, b and c under given constraints
        I.) Recursion
2. Interview prep II - 
    a.) Rows and column - Pending.
    b.) All distances - generate all routes(permutation) between n houses and find the average distances. 
3. Research work.